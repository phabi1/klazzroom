import { FindSpacesByUserHandler } from './find-spaces-by-user.handler';

describe('FindSpacesByUserHandler', () => {
  it('should be defined', () => {
    expect(new FindSpacesByUserHandler()).toBeDefined();
  });
});
