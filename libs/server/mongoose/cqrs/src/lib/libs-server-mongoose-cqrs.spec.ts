import { libsServerMongooseCqrs } from './libs-server-mongoose-cqrs';

describe('libsServerMongooseCqrs', () => {
  it('should work', () => {
    expect(libsServerMongooseCqrs()).toEqual('libs-server-mongoose-cqrs');
  });
});
