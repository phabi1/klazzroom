# libs-server-mongoose-normalize

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libs-server-mongoose-normalize` to build the library.

## Running unit tests

Run `nx test libs-server-mongoose-normalize` to execute the unit tests via [Jest](https://jestjs.io).
