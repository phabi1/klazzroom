# libs-api-gateway-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libs-api-gateway-core` to build the library.

## Running unit tests

Run `nx test libs-api-gateway-core` to execute the unit tests via [Jest](https://jestjs.io).
