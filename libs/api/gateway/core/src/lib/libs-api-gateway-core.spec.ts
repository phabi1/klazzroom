import { libsApiGatewayCore } from './libs-api-gateway-core';

describe('libsApiGatewayCore', () => {
  it('should work', () => {
    expect(libsApiGatewayCore()).toEqual('libs-api-gateway-core');
  });
});
