import { libsApiGatewayPlugin } from './libs-api-gateway-plugin';

describe('libsApiGatewayPlugin', () => {
  it('should work', () => {
    expect(libsApiGatewayPlugin()).toEqual('libs-api-gateway-plugin');
  });
});
