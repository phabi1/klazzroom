import { libsApiGatewayPluginKeycloak } from './libs-api-gateway-plugin-keycloak';

describe('libsApiGatewayPluginKeycloak', () => {
  it('should work', () => {
    expect(libsApiGatewayPluginKeycloak()).toEqual(
      'libs-api-gateway-plugin-keycloak'
    );
  });
});
