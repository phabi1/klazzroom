# libs-api-gateway-plugin-keycloak

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libs-api-gateway-plugin-keycloak` to build the library.

## Running unit tests

Run `nx test libs-api-gateway-plugin-keycloak` to execute the unit tests via [Jest](https://jestjs.io).
