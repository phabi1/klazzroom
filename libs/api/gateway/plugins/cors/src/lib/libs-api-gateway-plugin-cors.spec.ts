import { libsApiGatewayPluginCors } from './libs-api-gateway-plugin-cors';

describe('libsApiGatewayPluginCors', () => {
  it('should work', () => {
    expect(libsApiGatewayPluginCors()).toEqual('libs-api-gateway-plugin-cors');
  });
});
