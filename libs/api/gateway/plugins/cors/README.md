# libs-api-gateway-plugin-cors

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libs-api-gateway-plugin-cors` to build the library.

## Running unit tests

Run `nx test libs-api-gateway-plugin-cors` to execute the unit tests via [Jest](https://jestjs.io).
