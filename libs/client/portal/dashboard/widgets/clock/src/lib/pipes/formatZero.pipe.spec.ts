import { FormatZeroPipe } from './formatZero.pipe';

describe('FormatZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
