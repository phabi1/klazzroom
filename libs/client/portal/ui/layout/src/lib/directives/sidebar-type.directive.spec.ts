import { SidebarTypeDirective } from './sidebar-type.directive';

describe('SidebarTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarTypeDirective();
    expect(directive).toBeTruthy();
  });
});
