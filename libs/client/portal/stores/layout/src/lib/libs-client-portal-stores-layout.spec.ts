import { libsClientPortalStoresLayout } from './libs-client-portal-stores-layout';

describe('libsClientPortalStoresLayout', () => {
  it('should work', () => {
    expect(libsClientPortalStoresLayout()).toEqual(
      'libs-client-portal-stores-layout'
    );
  });
});
