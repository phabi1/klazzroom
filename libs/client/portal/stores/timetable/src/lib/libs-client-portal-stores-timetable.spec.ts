import { libsClientPortalStoresTimetable } from './libs-client-portal-stores-timetable';

describe('libsClientPortalStoresTimetable', () => {
  it('should work', () => {
    expect(libsClientPortalStoresTimetable()).toEqual(
      'libs-client-portal-stores-timetable'
    );
  });
});
