import { libsClientPortalStoresDashboard } from './libs-client-portal-stores-dashboard';

describe('libsClientPortalStoresDashboard', () => {
  it('should work', () => {
    expect(libsClientPortalStoresDashboard()).toEqual(
      'libs-client-portal-stores-dashboard'
    );
  });
});
