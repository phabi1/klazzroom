import { DashboardWidgetTypeDirective } from './dashboard-widget-type.directive';

describe('DashboardWidgetTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardWidgetTypeDirective();
    expect(directive).toBeTruthy();
  });
});
