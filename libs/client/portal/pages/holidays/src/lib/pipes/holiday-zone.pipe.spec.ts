import { HolidayZonePipe } from './holiday-zone.pipe';

describe('HolidayZonePipe', () => {
  it('create an instance', () => {
    const pipe = new HolidayZonePipe();
    expect(pipe).toBeTruthy();
  });
});
